import { AIRecommendationsPage } from "../AIRecommendationsPage";
import { ThemeProvider } from "../ThemeProvider";

export default function AIRecommendationsPageExample() {
  return (
    <ThemeProvider>
      <AIRecommendationsPage />
    </ThemeProvider>
  );
}
